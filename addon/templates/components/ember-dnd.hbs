{{yield
    (hash
        item=(component 'ember-dnd/item'
            insert=(action 'insertItem')
            destroy=(action 'destroyItem')
            dragStart=(action 'onDragStart')
            drag=(action 'onDrag')
            dragEnd=(action 'onDragEnd')
        )
    )
}}

{{ember-dnd/spacer baseClass=baseClass isHidden=isNotDragState}}
