{{yield
    (hash
        handler=(component "ember-dnd/handler"
            baseClass=baseClass
            insert=(action "insertHandler" this)
            destroy=(action "destroyHandler")
        )
    )
}}
